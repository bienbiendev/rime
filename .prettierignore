# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock

# Generated by `bun run rime:pipeline`; `pipeline-map.spec.ts` compares it byte for byte,
# so reformatting it here would make the suite fail against its own generator.
docs/pipeline-map.md
