<!--
  Minimal front page so SvelteKit has a route for `/`.

  This fixture's suite only exercises the API and the panel, but without it `/` 404s, and
  playwright's webServer waits for a 2xx/3xx before starting the run.
-->
<h1>rime</h1>
