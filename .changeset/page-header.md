---
'rimecms': minor
---

Changed: the page header puts the command palette in its middle, as a search box. A document's header is one row: the breadcrumb names the document, so the big title goes, and its actions, save, settings, status, the live and site links, sit on the right with the language switcher, its icon and the locale code, the same everywhere. The document's dates and id sit at the bottom of the page. The other locales are in the palette too, _Switch to <language>_.
