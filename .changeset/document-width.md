---
'rimecms': patch
---

Changed: a document's fields stop growing on a wide screen and centre themselves, at `60rem`. `--rz-document-width` on the panel, a collection's page or one document changes it; the page gutter stays the floor, and the collection list, the dashboard and the page headers are untouched.
